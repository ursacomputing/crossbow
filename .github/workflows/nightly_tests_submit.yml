name: Submit tests

on:
  push:
    paths:
      - '.github/workflows/nightly_tests_submit.yml'
  schedule:
    - cron: '0 5 * * *'

jobs:
  nightly_tests_submit:
    uses: ursacomputing/crossbow/.github/workflows/submit.yml@separate_nightlies
    with:
      report_type: tests