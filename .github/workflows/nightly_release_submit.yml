name: Submit releases

on:
  push:
    paths:
      - '.github/workflows/nightly_releases_submit.yml'
  schedule:
    - cron: '0 14 * * *'

jobs:
  nightly_releases_submit:
    uses: ursacomputing/crossbow/.github/workflows/submit.yml@master
    with:
      report_type: releases
