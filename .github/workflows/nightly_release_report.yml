name: Report releases

on:
  push:
    paths:
      - '.github/workflows/nightly_releases_report.yml'
  schedule:
    - cron: '0 20 * * *'

jobs:
  nightly_releases_report:
    uses: ursacomputing/crossbow/.github/workflows/report.yml@master
    with:
      report_type: releases
